# Verification

Local environment: Apple M4 Max, macOS arm64, Go 1.26.3. Date: 2026-09-08.

## Executed checks

- `go test -race ./...`: target boundaries, port validation/deduplication, bounded worker concurrency, cancellation, cached-neighbor evidence, snapshot roundtrip/diff, MAC classification and longest-prefix matching, mDNS SRV/A/TXT correlation, SSDP parsing, CSV formula escaping, terminal-control sanitation, and invalid CLI inputs.
- `go vet ./...` passed.
- `LANTERN_NETWORK_TESTS=1 go test -race ./pkg/scanner -run TestNetworkIntegration -v`: passed against a controlled localhost listener. Verifies actual ICMP, detection of the dynamically allocated TCP port, and the exact SSH banner.
- `go test ./pkg/vendors -bench BenchmarkLookup -benchmem -run '^$'`: 76.67 ns/op, 24 B/op, 2 allocations/op for warm parsed-string lookups, with 58,421 assignments loaded.
- Live quick scan of the en1 IPv4 /22: 1,022 addresses, 3,771 ms, two observed devices, one ICMP responder, at 256 workers. DNS/multicast were disabled. The ICMP send queue dropped 39 sends; TCP discovery continued. The report is local at `research/results/local-quick.json` and is excluded from distribution.

The initial broad scan found a blocking Darwin ICMP send. A goroutine dump identified `icmp.PacketConn.WriteTo` waiting for socket writability. Each send now has a 2 ms deadline; blocked sends are reported as incomplete coverage instead of holding the scan indefinitely. The failed run was explicitly stopped before the successful rerun.

These checks establish functional behavior and one-network performance, not exhaustive network discovery or a comparative Fing benchmark. Network isolation, filters, response latency, and neighbor cache state affect results. Linux runtime validation and a broader reference-device corpus remain outstanding.

## Final expanded checks

- Standard scan at the current 512-worker default: 1,022 addresses in **2,389 ms**, two observed devices; DNS and multicast enabled. No multicast advertisements arrived on this network. The OS ICMP send queue dropped 23 sends and that limitation was reported. Local snapshot: `research/results/local-standard.json`.
- Full **65,535-port** localhost scan: **861 ms**, 30 open TCP ports, no warnings. ICMP, DNS, and multicast disabled for this measurement. Local snapshot: `research/results/loopback-full-ports.json`.
- mDNS parser fuzzing: **886,080 executions** in two seconds, passed. This is a short malformed-packet exercise, not exhaustive protocol validation.
- A canceled /20 × 65,535-port plan returns in approximately 10 ms without allocating the host × port matrix.
- `govulncheck` against the current database: **zero reachable known vulnerabilities**. It also reported two findings in imported packages and eleven in required modules that this code does not appear to call. No claim is made that the dependency graph contains zero advisories.
- Updated dependencies to `golang.org/x/net v0.58.0`, `x/term v0.45.0`, and transitive `x/sys v0.47.0`; minimum Go version is now 1.25.
- Built local release archives for macOS and Linux, ARM64 and x86-64. These are build checks; only the macOS ARM64 executable has been run here. CI has been defined but has not run on a remote runner.

- The public-data refresh script completed end to end against IEEE and IANA, preserving the expected 58,421 and 5,889 assignment counts.
- The controlled ICMP/TCP/banner integration test passed again after the dependency upgrades.
- Human report rendering was checked at 80 and 120 columns, including oversized names and terminal escape sanitization.

## Recognition expansion

- Standard scan after recognition changes: **1,022 addresses in 2,297 ms**, two devices, one model identification from a live advertisement. Sixteen ICMP writes timed out and were reported. Full local record: `research/results/recognition-standard.json` (excluded from distribution).
- Controlled localhost mDNS integration reconstructs a previously unknown service type from separate enumeration, PTR, SRV, TXT, and A responses.
- Controlled UPnP integration verifies device/embedded-device UDN matching, URL-fetch deduplication, off-device rejection, no redirect following, body limits, and cancellation of slow HTTP responses.
- Pure tests cover TXT case folding/duplicate keys, scope-bounded followups, description parsing limits, identity conflicts and source attribution, exact catalog matches, and identity CSV output. All packages pass with the race detector; `go vet` passes.
- Short fuzz runs: 354,405 description-parser cases and 589,287 mDNS cases; both passed. These runs predated the toolchain change below; the final patched build's full tests and controlled network checks also passed.
- New HTTP/XML call paths made Go 1.26.3 standard-library advisories reachable to `govulncheck`. The project now requires patched **Go 1.26.8** (verified in the official Go release feed), and the final scan reports **no vulnerabilities found**. This supersedes the earlier toolchain requirement for the current build.
- Added 40 exact Cast model/manufacturer mappings from a pinned MIT-licensed PyChromecast source, with content hash and license retention.

## IPv6 expansion

- All package tests passed with the race detector on Go 1.26.8; `go vet ./...` passed. Added checks for scoped target parsing, IPv6 range bounds, Darwin/Linux NDP parsing, local-prefix/interface filtering, candidate limits, early option validation, AAAA correlation, scope-safe description URLs, and full address rendering at 60–140 columns.
- Controlled network tests passed on both IPv4 and IPv6: ICMP echo, TCP detection, SSH banners, split DNS-SD enumeration/PTR/SRV/TXT/A-or-AAAA replies. IPv6 HTTP tests verified bracketed Host headers, server banners, and UPnP descriptions.
- Live IPv6 standard discovery on en1 took **1,464 ms**, retained and probed one address, and produced no warnings. The observed device was the scanning machine, with ICMP, mDNS, local-interface, neighbor-cache, and TCP evidence. This validates the local scoped path; it does **not** establish discovery coverage of other IPv6 devices. Private report: `research/results/ipv6-standard.json`.
- mDNS fuzzing with IPv6 hit generation and AAAA follow-ups: **576,488 executions**, passed.
- Large IPv6 prefixes use observed candidates instead of address enumeration. Reports explicitly record this distinction. IPv4 and IPv6 scans remain separate; direct ARP/NDP and Linux runtime validation remain outstanding.
- Rebuilt all four release archives after IPv6 changes; macOS ARM64 archive executable smoke-tested. Linux artifacts remain cross-builds pending runtime verification.

## Direct ARP and Linux runtime

- Added direct Ethernet/IPv4 ARP: Linux AF_PACKET socket and macOS BPF backend, with nonblocking I/O, write/read deadlines, cancellation, burst pacing, and no promiscuous mode. Tests reject foreign recipients, wrong ARP/Ethernet fields, malformed/truncated records, unsolicited/duplicate addresses, and out-of-link targets. Fresh ARP mappings take precedence over stale cache values.
- macOS Go 1.26.8: all packages passed with the race detector; vet passed. ARP reply fuzzing ran **422,270 cases**; Darwin BPF record fuzzing ran **405,222 cases**, both passed.
- Linux ARM64 on OrbStack, official Go 1.26.8 Bookworm image pinned by digest: vet, all race tests, and controlled IPv4/IPv6 ICMP, TCP, SSH/HTTP banner, mDNS, and UPnP integration tests passed. An ARP-only integration test verified the container bridge gateway with cache lookup disabled. The built CLI then identified that gateway using only ARP in **314 ms** with no warnings; the protocol reply latency was **0.026 ms**. This single-target result is not a subnet benchmark. Local log: `research/results/linux-runtime.log` (excluded from distribution).
- The Linux test container had all capabilities removed except `NET_RAW`, no host mounts, and no published ports. `make linux-test` reproduces the workflow. CI includes the same Linux container test; remote CI has not yet run.
- macOS unprivileged ARP fallback completed in **53 ms**, reported BPF permission denial, retained two cache/local observations, and attempted zero network probes. `/dev/bpf*` requires root here, and `sudo -n` requires a password. Native macOS raw ARP exchange therefore remains **unverified**; its implementation is cross-built and its decoding/deadline-independent logic is tested.
- The final Linux rerun passed after adding early termination on send/read errors. Packaged macOS ARM64 and Linux ARM64 binaries were smoke-tested. The Linux release binary also ran in a container with **all capabilities removed** and correctly warned that ARP was unavailable, sending zero probes. That separate artifact test mounted only the extracted release directory read-only; no workspace or host credentials were mounted. Private result: `research/results/linux-arp-no-capability.json`.

## Hardware model catalog expansion

- Imported **606 identifiers / 886 assignments** from MIT-licensed AppleDB revision `95f799d28e45dc110ee2f25b7e3e6cf8c1124dae`. All **124 ambiguous identifiers** retain their source variants. Each of 1,905 input JSON records was verified against its pinned Git blob hash. Generated index: **46,306 bytes** compressed.
- Catalog tests verify an unambiguous Mac Studio, multi-year MacBook Pro identifiers, Apple TV hardware variants, excluded prototypes, unknown/malformed identifiers, immutable lookup results, input/source linkage, index SHA-256, and aggregate counts. Scanner tests preserve the advertised code and reject fuzzy matches, arbitrary TXT fields, and metadata from an unselected conflicting model. CSV candidate output and terminal ambiguity labels are covered.
- All package race tests and vet passed after fixing a test expectation to account for CSV quoting of comma-containing identifiers. Controlled IPv4/IPv6 DNS-SD tests reconstructed a catalog-recognized model from separate SRV/TXT/A-or-AAAA packets.
- Live en1 IPv6 discovery completed in **1,349 ms**, one observed address, no warnings. Its advertised `Mac16,9` now has the catalog candidate **Mac Studio (M4 Max, 2025)** and Apple manufacturer provenance. Raw identifiers remain in the report. Private snapshot: `research/results/apple-model-live.json`.
- Warm `pkg/models.Lookup("Mac16,9")`: **195.6 ns/op**, 376 B/op, 7 allocations/op on Apple M4 Max. This includes building independent result records with escaped source URLs.
- Rebuilding from the pinned local archive and retrieval date produced byte-identical index and source metadata. A modified Git blob inventory was rejected before output creation. The importer executes no upstream code.
- The downloader/importer also passed end to end using a fresh temporary directory; the newly fetched pinned inputs reproduced both output files byte-for-byte.
- Final Linux ARM64 container checks passed with the catalog, including full race/vet and network integration tests. All four release archives were rebuilt and checked for the retained AppleDB license. No cross-brand identifier collisions occur in the imported index.

## ICMP completion and send recovery

- Added per-target sequence validation and reply deduplication, immediate completion when every echo target replies, and a single retry for local send timeouts/`EAGAIN`/`ENOBUFS`. Retry work shares at most 100 ms (or the shorter probe timeout). JSON counters distinguish attempted targets, accepted sends, retries, recovered writes, remaining failures, and unique echo responders.
- Deterministic socket fixtures verify IPv4 and scoped IPv6 completion, the full wait for silent targets, duplicate/foreign/wrong-nonce/wrong-sequence rejection, recoverable versus permanent send failures, retry-budget exhaustion, cancellation, read errors, and failed deadline setup without a blocked reader. All race tests and vet passed on macOS and Linux ARM64.
- Controlled loopback TCP/ICMP/banner integration now completed in **0.02 seconds combined on macOS** and **0.04 seconds combined on Linux**, rather than spending the default 300 ms echo wait per fully responding address. The pre-change Linux run took 0.66 seconds for both address families. These are test-suite wall timings, not TCP throughput measurements.
- A saved pre-change build scanned the local en1 /22 in **4,060 ms**, warning that 1,006 of 1,022 echo sends timed out. The initial revised build took **1,322 ms**, accepted 1,003 echo writes, retained one echo responder, and reported 19 unsent addresses after its retry budget expired. Both observed two addresses overall.
- Alternating follow-up pairs: old/new **4,019 / 1,052 ms**, then **3,853 / 903 ms**. Each new run recovered **40** initially failed echo writes; final unsent counts were **3** and **137**. Both old runs reported 1,006 unsent echo probes; every run observed two addresses overall. Both binaries used Go 1.26.8 and the same dependency versions. The saved baseline binary was built from the preceding worktree before its commit, so its Go VCS metadata reports the earlier parent plus dirty state.
- The varying unsent counts show that local queue/cache state materially affects coverage. These observations establish recovery and local timing improvements, not a universal speedup, complete discovery, or a Fing comparison. Private records: `research/results/echo-comparison.json`, `echo-before.json`, `echo-after-initial.json`; Linux runtime log: `research/results/echo-linux-runtime.log`.
- Rebuilt all four release archives. The packaged macOS ARM64 binary completed an IPv6 loopback ICMP-only scan in **8 ms**, with one attempted/sent/responding address, zero retries/failures, and no warnings. Private record: `research/results/echo-release-loopback.json`.


## Interactive watch dashboard (2026-09-08)

- Passed on macOS ARM64 and in the isolated Linux ARM64 container, alongside all existing race/protocol tests. The macOS pseudo-terminal suite also passed with a race-instrumented CLI binary. All four macOS/Linux ARM64/x86-64 release archives rebuilt successfully. `govulncheck` reported no vulnerabilities after adding the MIT-licensed Unicode segmentation dependency.

- `go test -race ./...` covers grapheme-safe cell widths and truncation, CJK/emoji/combining text, terminal and bidi-control sanitation, stable address selection across refreshes, model search, activity history bounds, all warning retention, scrolling, partial-report change semantics, and fragmented UTF-8/escape/bracketed-paste input.
- Frame checks cover 1×1 and 20×6 resize prompts and 36×10, 60×15, 80×24, 120×40, and 180×55 result layouts. Every frame stays within the terminal's row count and reserves its last column to prevent wrapping. Scoped IPv6 detail values wrap without losing their address.
- `make terminal-test` builds the CLI and runs `scripts/test-watch-pty.py` using real pseudo-terminals. It exercises synthetic selection/search/details/activity, pause and forced/automatic refresh, change detection, live resize, bracketed paste, quit/Ctrl-C while scanning, no-color settings, a loopback scan with snapshot saving, SIGTERM, invalid-scan and save-error cleanup, plain/dumb-terminal fallback, and redirected JSONL parsing. The test restores and compares terminal flags and checks alternate-screen/cursor cleanup. On Darwin it ignores only the kernel's PENDIN bookkeeping bit, which a standard Python raw/restore round trip also sets.
- The dashboard coalesces progress/discovery events independently of terminal rendering and joins a cancelled scanner before restoring the terminal. Reports and errors use a reliable completion channel. UI state stays in `internal/ui`; `pkg/scanner` has no terminal dependency.
- Interactive demo tests send no network traffic; the real scan test is restricted to IPv4 loopback with network probes disabled. These checks do not establish visual compatibility with every terminal emulator or screen reader. Use `--plain` for appended output.


## NetBIOS discovery (2026-09-08)

- Unit/race tests cover exact wildcard wire encoding, full-record length validation, all truncation boundaries, response flags/type/class/counts/owner validation, wrong sender/port/transaction rejection, duplicate suppression, early completion, silent deadlines, stopped writes, read/deadline failures, cancellation with partial results, 32-request pacing, byte-preserving name sanitation, workgroup/host distinction, source claims, and independently scheduling requested TCP ports on NetBIOS-only responders.
- A controlled UDP loopback exchange passed on macOS ARM64 and Linux ARM64. `FuzzNetBIOSReply` completed 2,440,511 inputs in ten seconds without a failure.
- The Linux test image includes Samba only as a test dependency; no Samba source/library is linked or bundled with Lantern. `scripts/test-netbios-samba.sh` runs `nmbd` inside the disposable container without starting an SMB file server or publishing ports. Samba 4.17.12-Debian supplied `LANTERN-SAMBA` and `LANTERN-LAB`; completed node-status-only scans took 4–5 ms. The test verifies name provenance, preserved group registration, no manufactured TCP ports, deep/inspect defaults, and the explicit disable override. Startup readiness is bounded and the daemon is stopped by the script's cleanup trap.
- These results establish protocol interoperability with a real Samba implementation, not Windows hardware coverage or a universal latency claim. Modern hosts with NetBIOS disabled will not answer. Broad real-network identification/coverage comparisons remain pending.


## Snapshot and watch comparisons (2026-09-08)

- Added requested scan coverage to schema-1 reports, structured before/after change fields, name/model/vendor/workgroup comparisons, and responsive-to-cached evidence transitions. Tests cover legacy snapshots, cancellation, interface changes, different port plans, case/order/duplicate normalization, control-safe detail strings, coverage ownership, persistence, and dashboard identity activity.
- `scripts/test-snapshot-cli.py` checks the actual CLI against saved JSON fixtures without network traffic; it is included in CI and the Linux runtime suite.
- The full-port-coverage benchmark compares 1,024 device records with one observed open port/name each and all 65,535 requested ports. Shared bitsets avoid rebuilding a large port map for every device. On the local Apple M4 Max, the optimized comparison measured approximately 0.45 ms with 344 KiB allocated; this is a comparison benchmark, not a network-scan timing.

- macOS ARM64 race/vet and snapshot CLI checks passed, as did the isolated Linux ARM64 race/protocol/terminal/Samba/snapshot suite. All four macOS/Linux ARM64/x86-64 release archives rebuilt successfully.


## Installation and release packaging (2026-09-08)

- The module path is `github.com/coupez/lantern`; CLI and core imports use that canonical path. `lantern version` reports an explicit release build label or the module version embedded by `go install`, with a development fallback for checkout builds.
- `scripts/test-install.py` constructs a file-only module proxy from the current source and cached dependencies. It uses isolated module/build caches and installs outside the checkout, verifies the installed version/demo, compiles a separate consumer importing scanner/vendor/model APIs and their embedded data, and stages `make install` with spaces in `PREFIX`/`DESTDIR`. A rejected release-version input is checked to ensure it is treated as data. No system installation or remote publication occurs.
- Release archives include linked offline docs, license notices, public dataset provenance JSON, and the Fing inspection inventory. Checksums use basenames so they can be verified from a download directory. `scripts/test-release.py` checks the four archives' hashes, safe regular-file entries, normalized timestamps/ownership/modes, platform/architecture headers, local Markdown links, licensing/provenance, and the host-compatible executable's version/demo/vendor/model commands.
- GitHub API checks found the repository private and its release list empty. Remote public installation is not established by the local file-proxy test. macOS Developer ID signing/notarization is not configured. No repository-visibility change or release publication was performed in this milestone.

- Installation, external-core imports, temporary prefix staging, and release-version validation passed on macOS ARM64 and Linux ARM64. The Linux protocol/terminal/Samba/snapshot suite passed after the module-path change. All four local archives passed structural, checksum, licensing, provenance, and offline-link validation; the packaged macOS ARM64 executable also passed its offline commands.

## Bounded banner reads and core port plans — 2026-09-08

- Core callers now receive deduplicated probe execution as well as deduplicated coverage metadata; the supplied port slice remains unchanged, and port zero fails before probing. The existing cancelled full-port-plan test still passes without materializing a host/port Cartesian product.
- Banner reads run with at most four exchanges per device and `min(32, Concurrency)` across a scan. Connection setup and I/O share one deadline. Tests cover both concurrency limits, every eligible port being attempted, later responsive ports after earlier stalls, queued cancellation, active-read cancellation, unsupported deadlines, parent deadlines, scoped IPv6 dialing, and zone-free HTTP Host headers.
- Fragmented HTTP/SSH responses are assembled across reads. Parser tests cover FTP/SMTP greetings, EOF greetings, terminal controls, body/header separation, line/count/byte bounds, and oversized fields. In-memory pipe fixtures exercise actual blocking reads and deadlines without network access.
- On macOS ARM64 (Apple M4 Max), `go test ./pkg/scanner -run '^$' -bench BenchmarkStalledBanners -benchtime=20x -count=3` measured eight simulated stalled exchanges with a 2 ms timeout: serial **19.97–20.33 ms/op**, bounded parallel **5.06–5.12 ms/op**, approximately **4× faster**. This measures scheduling of a specific stalled-banner workload, not overall scan throughput or real-network latency.
- macOS vet, all race tests, and controlled IPv4/IPv6 socket integration passed. The Linux ARM64 container passed vet, all race tests, protocol/ARP integration, independent core installation, terminal/watch and snapshot CLI checks, and live Samba interoperability. Local container log: `research/results/banner-linux-test.log` (excluded from distribution). Privileged macOS ARP and x86-64 runtime verification remain pending.

## HomeKit recognition and TXT fidelity — 2026-09-08

- Added HomeKit advertised names/models and interpretation of 36 category codes, with a pinned protocol-reference URL in category claims. Tests verify no manufacturer inference, no Cast/Apple catalog crossover, no protocol-ID-to-MAC promotion, deterministic conflicting-category handling, exact service matching, malformed/unknown categories, and preservation of input advertisements.
- TXT parsing now rejects malformed keys instead of removing characters to create a valid key, and retains values until interpretation. Tests cover first-duplicate semantics, embedded `=`, control-bearing categories/models, and terminal-safe details with protocol provenance. A wire-level malformed model cannot turn into a catalog match through display sanitization.
- Real split PTR/SRV/TXT/A and AAAA socket tests on macOS and Linux ARM64 verify HomeKit model/name/category recognition, without reporting advertised ports as verified open ports. The first run exposed lowercase follow-up replies overwriting PTR instance spelling; the fix preserves the original spelling and the repeated tests pass.
- macOS vet and all race tests passed. Linux vet, all race tests, IPv4/IPv6 protocol integration, direct ARP, installation/import checks, watch/PTY checks, snapshot CLI checks, and actual Samba interoperability passed. Local log: `research/results/homekit-linux-test.log` (excluded from distribution).
- A local advertisement/neighbor scan (`--ports none --no-icmp --no-dns --json`) completed in **1,022 ms**, observed two addresses, and reported no warnings. It saw AirPlay/RAOP and other services but no HomeKit advertisements. This supports continued discovery operation on that network; it does **not** establish physical HomeKit-device interoperability. Local observations: `research/results/homekit-local-scan.json` (excluded from distribution).

## Local capability diagnostics — 2026-09-08

- Replaced static `doctor` claims with actual interface/target-selection, ICMP socket, multicast configuration, raw ARP resource, and neighbor-table checks. Added `--interface`, JSON output, reasons/hints for individual failures, cancellation, output-error handling, and the reusable `scanner.Diagnose` API. No discovery packets or ARP reads are used by diagnostics; remote TCP/DNS/device reachability remains explicitly untested.
- Unit tests cover failure isolation, exactly-once resource closure, close errors, cancellation before/during checks, skipped remaining work, flag validation before inspection, structured/plain output, untrusted error-text sanitation, writer failures, and partial cancelled reports. Context-aware route lookups retain the existing scan selection rules. Multicast setup now propagates deadline configuration errors.
- On macOS ARM64, a host diagnostic report completed in **10 ms**: IPv4/IPv6 ICMP and multicast setup succeeded, raw ARP returned permission denied, and neighbor-table reads succeeded. Inside the command sandbox, ICMP binding and automatic route selection were restricted; doctor retained these failures in a successful structured report. Reports: `research/results/doctor-macos.json` and `research/results/doctor-sandbox.json` (excluded from distribution).
- macOS vet, all race tests, doctor CLI checks, and controlled IPv4/IPv6 protocol tests passed. Linux ARM64 passed the full race/protocol/ARP/Samba/watch/snapshot/install suite and doctor checks with `CAP_NET_RAW`. A second container with all capabilities removed verified a valid IPv4 target and an explicit permission-denied raw ARP result. Both capability configurations are now part of `make linux-test`; basic doctor CLI checks also run in both CI OS jobs. Local log: `research/results/doctor-linux-test.log`. Remote CI execution remains unverified.

## Interface-bound local evidence — 2026-09-08

- Automatic IPv4 CLI scans now retain the default-route interface alongside the selected prefix. The additive `AutoTarget4` core API returns both values; `AutoTarget` retains its prefix-only behavior. Tests cover identical subnets on two adapters, order-independent default-route selection, explicit overrides, and ambiguous/missing-interface fallback.
- IPv4 interface names are validated before probes, and selected-interface filtering now applies to IPv4 cached neighbors and local-address evidence as well as IPv6. macOS tests on two actual active IPv4 interfaces verify that an address on another adapter does not become local-interface evidence, while the selected adapter's address still does. A cache/local-only CLI run completed in **27 ms**, recorded the selected `en1` interface, attempted zero probes, and reported no warnings (`research/results/interface-local-scan.json`, excluded from distribution).
- A controlled Linux command confirmed that `ip neigh show dev NAME` omits the `dev` column. The earlier IPv6 path expected that column and could silently discard scoped cache rows. Both address families now read the interface-bearing table and filter during parsing. BSD/macOS and Linux parser tests cover duplicate IPs with different MACs on two links, reordered rows, missing interface provenance, and a hostname equal to `on`.
- The dedicated Linux container fixture creates two temporary veth interfaces and static IPv4, global IPv6, and link-local IPv6 neighbor entries. Actual CLI scans verify the selected MAC for the same IP on each interface, retained IPv6 zones, cached-only evidence, zero probes, and exclusion of an address present only on the other link. Links are deleted by the fixture and the container is removed. This container receives only `CAP_NET_ADMIN`, has no host networking/mounts, and is now part of `make linux-test`.
- macOS vet and all race tests passed. Linux ARM64 passed vet, all race tests, socket/ARP/Samba integration, doctor checks with and without raw-socket permission, terminal/watch checks, snapshot/installation checks, and the new real neighbor-table fixture. Log: `research/results/interface-linux-test.log` (excluded from distribution). Physical multi-adapter Linux and privileged macOS ARP testing remain separate from this container coverage.

## Incremental enriched events — 2026-09-08

- Added independently owned `device_update` payloads as devices finish enrichment, before scan-level `done`. Explicit progress phases distinguish discovery/port TCP jobs from processed-device enrichment counts. The initial discovery snapshot remains unchanged. Core tests retain and mutate nested event/report data in both directions, verify serialized callbacks and deterministic lifecycle constraints, and check deep-copy handling of advertisements, identity claims, model candidates, names, ports, and evidence.
- A controlled loopback HTTP fixture holds one device's banner response open. On macOS and Linux ARM64, another device's enriched update arrives while that connection remains blocked, and the report finishes only after release. Discovery is injected in this fixture; the stalled banner uses a real socket. Log: `research/results/live-events-linux-stall.log` (excluded from distribution).
- Watch mailbox/frame tests verify names and ports appear during the first scan, producer-owned values cannot mutate the UI's copies, identifying progress renders, and later refreshes preserve the previous completed report. The ordinary CLI renders phase changes immediately, bypassing its within-phase output throttle. JSON/CSV and non-color report-only output skip unused event snapshots.
- Actual JSONL CLI tests on macOS and Linux use a loopback TCP listener and verify discovery/ports/enrichment phases, initial observation, matching normalized update/report records, `done` ordering, and initial-event stability. `scripts/test-event-stream.py` is included in CI and the Linux container suite.
- macOS vet, race tests, JSONL tests, and the complete pseudo-terminal watch suite passed. Linux ARM64 passed the full race/protocol/ARP/Samba/watch/snapshot/install/diagnostic suite and the duplicate-interface neighbor fixture with the new event stream. Log: `research/results/live-events-linux-test.log` (excluded from distribution). The explicit stalled-banner regression additionally passed on both platforms.

## Multicast failures and completeness warnings — 2026-09-08

- mDNS and SSDP now retain decoded observations while reporting unexpected receive failures. Ordinary receive deadlines and cancellation remain quiet; query timeouts remain errors because the question was not sent successfully. Short datagram writes are treated as failures, and SSDP hop-limit configuration errors are propagated.
- Reaching the existing 512-packet cap or mDNS's 128-query cap adds a bounded completeness warning. Packet counts include ignored/malformed datagrams. Query exhaustion still allows pending replies to be processed. Combined receive/budget warnings preserve the original error for `errors.Is` and render as one readable line.
- Scripted-transport tests cover partial replies followed by unexpected EOF/close, quiet deadline expiry, initial/follow-up query failures, short writes, cancellation before I/O and during receive, packet limits, query limits, and combined limits/errors. They use no network traffic.
- New real IPv4/IPv6 SSDP tests inspect the M-SEARCH request, receive a reply, close the actual client socket, and verify both the retained advertisement and the reported close error. macOS and Linux ARM64 passed these tests plus the existing protocol tests. macOS vet/all race checks and the full Linux scanner/CLI/PTY/install/diagnostic/interface-isolation suite passed. Logs: `research/results/multicast-errors-macos.log` and `research/results/multicast-errors-linux.log` (excluded from distribution).
- A normal local multicast/neighbor scan with TCP, ICMP, reverse DNS, and description requests disabled completed in **1,057 ms**, observed two addresses, and reported no warnings. This checks ordinary collector completion on that network; it is not an accuracy benchmark or proof of exhaustive discovery. Report: `research/results/multicast-errors-local.json` (excluded from distribution).


## Partial reports and broken output streams — 2026-09-08

- The CLI now publishes/saves the aggregate after the engine reports that every TCP probe failed. `Report.Error` / JSON `error` marks the failure; exit status remains 1. Validation/setup errors do not publish or replace snapshots. Failed later snapshots cannot erase earlier device fields or declare missing addresses.
- `scanWithOutput` tests run the real engine with a permission-denying dialer and a retained neighbor, round-trip the saved and printed report, verify cancellation and worker joining on output failure, suppress subsequent stream writes, preserve independent saves, and retain simultaneous scan/save/report errors. Full macOS `go vet ./...` and `go test -race ./...` passed.
- The checked-in `scripts/test-event-stream.py` now closes the stdout pipe before launch and verifies status 1 plus a cancelled saved snapshot. It also holds a real HTTP banner read with a 30-second deadline, sends SIGINT after receiving the request, and verifies connection closure and equal partial JSONL/snapshot aggregates within a five-second process bound. Both cases passed on macOS ARM64 and Linux ARM64.
- Actual macOS sandbox TCP denials against the machine's selected local IPv4 address verified JSON, JSONL, CSV, and plain output retain the local-interface observation and save the failed report, with status 1. An actual pseudo-terminal watch under the same denial saved its failed snapshot and restored terminal attributes, cursor, and screen before exiting 1. The ordinary macOS pseudo-terminal suite also passed.
- `make linux-test` completed successfully, including race/vet, protocol socket fixtures, canonical module/install checks, the expanded JSONL tests, pseudo-terminal/snapshot/Samba checks, doctor with/without raw capability, and isolated veth neighbor-table fixtures. Local log: `research/results/partial-output-linux.log` (ignored development evidence, not a distribution artifact).

These checks establish cleanup and partial-result behavior, not broader physical-device discovery accuracy. Optional per-protocol failures still appear as warnings and requested coverage is not a guarantee that all protocols succeeded.


## Direct IPv6 neighbor discovery — 2026-09-08

- Added opt-in `--ndp` and core `Options.NDP`, `NDPResult`, and `Engine.NDPSource`. Ethernet NS/NA shares the existing non-promiscuous macOS BPF/Linux packet transport with ARP. Fresh NDP mappings override conflicting cached MACs, carry `ndp` responsiveness evidence and scoped link-local addresses, and participate in requested-coverage comparisons. Doctor checks the actual NDP descriptor separately.
- macOS race tests cover multicast NS addressing/checksum construction, NA validation with checksums recomputed after malformed-field changes, truncation, unknown/duplicate options, extension headers and fragment rejection, source/interface boundaries, unsolicited/foreign/duplicate replies, early completion, pacing, cancellation, partial read errors, initial write and deadline failures, fresh evidence, and IPv4 rejection. Full macOS vet/race checks passed, with focused reruns for subsequent NDP changes.
- The checked-in `scripts/test-ndp-linux.py` creates a veth pair inside the dedicated container. Real Linux kernel responses verified global and link-local solicitation, fresh MAC precedence over deliberately wrong permanent neighbor entries, no reliance on TCP/echo/multicast-service discovery, selected-interface evidence, completion before a two-second response deadline, finite-range scanning, off-prefix exclusion, and doctor availability. An additional targeted run verified a /64 uses exactly the two discovered candidates rather than enumerating addresses.
- The same fixture with only `NET_ADMIN`, without `NET_RAW`, verified NDP permission warnings, retained cached-only observations, zero direct attempts, and the doctor's permission-denied result after successful IPv6 target selection. The raw-exchange fixture adds only `NET_RAW`; all links are removed in `finally` and the disposable containers use no host networking, mounts, or published ports.
- `make linux-test` passed the entire regression suite, including existing actual ARP exchange after the shared transport change, protocol socket fixtures, race/vet, installation checks, JSONL/broken-pipe/cancellation cases, pseudo-terminal and snapshot checks, Samba, doctor capability variants, scoped neighbor tables, and the new NDP fixtures. Log: `research/results/ndp-linux-test.log` (ignored development evidence).
- A macOS scan with direct raw access denied retained one sparse IPv6 observation with zero direct probes and an NDP warning in 16 ms. Doctor reported NDP unavailable. These are fallback checks; privileged macOS BPF NDP exchange, physical IPv6-device coverage, x86-64 runtime, and accuracy/speed comparisons across broader networks remain unverified.


## ESPHome firmware recognition — 2026-09-08

- Added direct `_esphomelib._tcp` discovery, advertised friendly-name/instance fallback, an explicit ESPHome firmware label, and the reported firmware version. Build-board, platform, and project fields remain source-linked claims rather than retail model/manufacturer guesses. TXT MACs do not replace observed MACs; advertised API ports remain unverified; import URLs are not fetched.
- Protocol semantics were checked against ESPHome revision `1ce0bed3f672d3a4699dad0cbfd8617c3b8950e5`, `esphome/components/mdns/mdns_component.cpp`, SHA-256 `2b04cfea602306e369ff45660d3e25d369f082de47d11800a58e160bfa8deae0`. The downloaded implementation remains ignored research input; no upstream code is incorporated.
- Unit/race tests cover exact service/protocol boundaries, DNS case-insensitivity, source-linked claims, friendly-name fallback, preserved raw TXT, control/length handling, rejection of board-to-hardware catalog guesses, deterministic competing firmware instances, no version mixing across sources, specific type-hint precedence, clone ownership, and coverage-aware firmware version diffs. CSV tests check both appended firmware columns, empty identities, and formula protection.
- Controlled split-reply DNS-SD exchanges passed on macOS and Linux for both IPv4 and IPv6. The ESPHome fixture ignores service enumeration, proving that the direct query works; it sends PTR/SRV/TXT/address responses separately and verifies the resulting identity, generic type hint, and lack of invented MAC/open-port evidence.
- Plain output and inspector show firmware, watch search includes firmware/version, and a 36-column rendering test exposed an existing report-summary overflow. Totals now stack and the response summary wraps at narrow widths. macOS vet/race and the actual pseudo-terminal suite passed. `make linux-test` passed the full suite, including DNS-SD fixtures, install/module checks, JSONL/cancellation, pseudo-terminals, snapshots, Samba, diagnostics, neighbor isolation, and ARP/NDP runtime checks. Log: `research/results/esphome-linux-test.log` (ignored development evidence).
- A normal local multicast/neighbor scan completed in 1,055 ms with two addresses and no warnings. It observed no ESPHome device, so physical ESPHome interoperability remains pending; the timing is not a comparative discovery-accuracy benchmark. Local artifact: `research/results/esphome-local-scan.json`.


## Four-platform packaged runtime and x86-64 Linux — 2026-09-08

- `make linux-test LINUX_PLATFORM=linux/amd64` passed the complete suite on the ARM64 Docker daemon using its installed x86-64 translation support. The actual Go toolchain reported `linux/amd64`, `uname -m` reported `x86_64`, and executed CLI diagnostic records were asserted to report `amd64`. All vet/race, IPv4/IPv6 protocol, install/core-consumer, JSONL/cancellation, terminal, snapshot, Samba, ARP, NDP, neighbor-scope, and capability-denial cases passed. No timeouts or assertions were relaxed for translation.
- Native `make linux-test` also passed through the new runner, reporting `linux/arm64` and `aarch64`. `scripts/test-linux.sh` validates the optional platform before Docker use, passes it to every build/run, and uses `lantern-linux-test:amd64` / `lantern-linux-test:arm64` tags. Unsupported input was verified to fail with status 2. Shell syntax and Python compilation checks passed.
- Local `arch-check` release archives were built for all four supported targets. `python3 scripts/test-release.py arch-check --linux-containers --macos-amd64` verified executable headers, checksums, licenses/provenance, offline links, and executed every archive's binary. Version, actual diagnostic architecture, offline MAC/model lookups, JSONL lifecycle/broken-pipe/SIGINT cleanup, pseudo-terminal restoration/navigation, and snapshot comparisons passed on each target. Linux archive runs used `--network none --cap-drop ALL` and received only the verified binary through stdin.
- The macOS x86-64 binary ran through installed Rosetta (`sysctl.proc_translated=1` in an explicitly selected x86-64 process). The macOS ARM64 binary ran natively. Linux ARM64 ran in the local VM/container environment; Linux x86-64 ran under translation. These results do not establish native Intel/AMD hardware performance, physical adapter compatibility, privileged macOS BPF exchange, or broad LAN recognition accuracy.
- Logs are ignored local research artifacts: `research/results/linux-amd64-runtime.log`, `research/results/linux-native-platform-runtime.log`, and `research/results/packaged-four-platform-runtime.log`. SHA-256 values for the four executed binaries are recorded in `research/results/packaged-runtime-binary-hashes.json`. Test commands and scope are documented in [platform testing](platform-testing.md). No release was published.


## Full-port aggregation speed and completeness — 2026-09-08

- Removed the redundant scan through an address's growing open-port slice. The existing owned port plan already guarantees unique jobs across discovery and the remaining-port phase, and only TCP jobs pass a nonzero port into aggregation. Collection now appends directly and retains final sorting; no per-device deduplication map or allocation for unopened ports was added.
- `TestFullPortScanHasUniqueJobsAndOwnedResults` runs all 65,535 ports on two addresses with duplicate caller input and discovery overlaps. It verifies all 131,070 address/port jobs occur exactly once, each complete report is sorted, coverage stays unique, and mutating initial/final event port slices cannot alter the aggregate. Cancellation checks verify bounded additional in-flight jobs and unique partial observations. macOS vet and all race tests passed.
- Five single-iteration synthetic samples against reference engine `13f34df` measured a median 1,689.763 ms before versus 58.240 ms after with 512 workers (29.0×); one worker measured 1,853.422 ms versus 17.903 ms (103.5×). The reference was an isolated temporary source copy with the same benchmark, and neither compared run used CPU profiling. Every port is reported open immediately by the injected dialer; this measures core overhead without socket latency. See [performance measurements](performance.md) for the workload and reproduction command.
- The real `scripts/test-full-ports.py` fixture passed against an owned loopback listener on port 65,535, with all 65,535 requested ports represented in coverage, the listener detected, sorted unique observations, and no warnings. Reported times were 1,094 ms on macOS ARM64, 454 ms on Linux ARM64, and 623 ms on translated Linux x86-64. These runs verify the full range; they are not controlled cross-platform speed comparisons or the all-open synthetic workload.
- Both complete Linux architecture suites passed, including race/vet, protocol sockets, installation/core imports, JSONL/terminal/snapshot/Samba checks, fresh ARP/NDP, scoped neighbor tables, denied capabilities, and the new real full-port fixture. The fixture is also included in macOS/Linux CI. Local logs: `research/results/full-ports-linux-arm64.log` and `research/results/full-ports-linux-amd64.log` (ignored development evidence).

## Partial discovery comparisons — 2026-09-08

- Added optional, sorted `incomplete_methods` to schema-1 reports. Discovery errors and collection limits retain their method independently of the bounded warning text, including sparse IPv6 seed failures and candidate truncation. Requested coverage remains separate; cancellation retains its existing marker.
- Snapshot/watch comparisons suppress missing addresses and response-state changes after reported discovery failures, and suppress only dependent device fields. Unaffected fields and newly observed addresses remain comparable under existing scope rules. Unknown future methods suppress all field comparisons. Scan-level changes expose failure/recovery; legacy reports retain observational fallback.
- macOS ARM64 vet and all race tests passed. Tests cover warning saturation, retained partial results, persistence, multiple methods, cancellation, sparse IPv6 transfer, selective field suppression, unknown methods, normalized immutable sets, recovery, and watch activity. The rebuilt CLI passed the expanded saved-snapshot fixture.
- The full Linux ARM64 container suite passed, including vet/race, protocol sockets, install/core imports, JSONL cleanup, full-port scanning, pseudo-terminals, snapshots, Samba, diagnostics, interface isolation, and raw ARP/NDP. Actual NDP permission denial retained cached observations and emitted exactly `incomplete_methods: ["ndp"]`; successful kernel exchanges emitted no incompleteness marker. Local log: `research/results/partial-discovery-linux.log` (ignored development evidence).
- This metadata tracks reported discovery failures, not silent hosts or unsuccessful reverse-DNS/banner/description enrichment. It does not establish exhaustive discovery or physical-device disappearance. See [snapshot semantics](snapshots.md).

## Shelly identity discovery — 2026-09-08

- Added direct `_shelly._tcp` queries, instance-name/generation observations, and a generic smart-home type hint. With descriptions enabled, the advertised service triggers a read-only `/shelly` request pinned to the discovered address and port. Selected model/name/firmware fields and build/application/profile/reported-MAC claims retain endpoint provenance; no retail catalog, manufacturer, observed MAC, or verified TCP-port inference is added.
- Shelly and UPnP share the existing four-target/per-device deadline. The shared HTTP transport retains disabled proxy/redirect/compression behavior, 16 KiB response headers, and caller cancellation. Shelly documents are limited to 16 KiB, selected strings to 2 KiB, and duplicate keys/invalid types/trailing data/incomplete identities are rejected. Failed reads preserve discovery observations.
- macOS ARM64 vet and all race tests passed. Controlled IPv4/IPv6 socket fixtures ignored service enumeration, returned PTR/SRV/TXT/address records separately, and then served the identity endpoint at the advertised port. Other socket fixtures checked deduplication, shared UPnP/Shelly request and time limits, oversized headers/bodies, redirects, unauthenticated failure, invalid JSON, peer pinning, and connection closure on deadline/cancellation. Log: `research/results/shelly-macos-network.log`.
- The full Linux ARM64 suite passed, including the same Shelly/UPnP socket cases, race/vet, install/core-import checks, JSONL cleanup, full-port CLI scanning, pseudo-terminal/snapshot/Samba tests, diagnostic capability variants, interface isolation, and actual ARP/NDP. Log: `research/results/shelly-linux.log`. The rebuilt macOS CLI also passed the snapshot fixture.
- A local multicast/neighbor scan with TCP sweeps, ICMP, and reverse DNS disabled completed in 1,055 ms, observed two addresses, and reported no warnings or incomplete methods. It observed no Shelly advertisement. Physical Shelly interoperability and Gen1 discovery remain unverified; this timing is not a comparative performance or discovery-accuracy measurement. Local observations: `research/results/shelly-local-scan.json`. All listed logs/reports are ignored development evidence.

## Large-result watch rendering — 2026-09-08

- Avoided building device search text for empty queries; cached current-report search/filter results and the selected wrapped inspector. Completed reports and live device events invalidate derived views, while progress-only mailbox updates reuse them. Visible service rows format only enough tokens for their column; full search and inspector data remain intact.
- Added synthetic redraw and first-view benchmarks. For 16 devices with all 65,535 ports open, median steady list redraw improved from 294.937 ms to 0.143 ms; inspector redraw from 232.933 ms to 0.024 ms. Three samples used three iterations each at 120×40, the same fixtures and untimed initial frame, and reference revision `129ba3c`. No network traffic, terminal writes, CPU profiling, or concurrent heavy tests were included. First search/layout still measured 95.004/168.834 ms after invalidation; [performance notes](performance.md#watch-redraws-with-large-results) state the workload and memory tradeoff.
- Regression tests cover width-limited output equivalence for Unicode/control strings, full-port search and last-port inspector access, current-report replacement, first-scan device updates, progress-only reuse, query changes, selected-device changes, resize wrapping, and firmware search refreshes.
- macOS ARM64 vet and all race tests passed. The actual pseudo-terminal suite passed selection/filter/inspector/pause/refresh/activity/resize/paste/quit, no-color operation, cancellation, snapshot errors, loopback scanning, and terminal restoration. Log: `research/results/watch-large-macos-pty.log`.
- The full Linux ARM64 suite passed, including race/vet, the new cache tests, protocol socket fixtures, install/core imports, JSONL cleanup, full-port scanning, pseudo-terminals, snapshots, Samba, scoped neighbors, diagnostic capability variants, and ARP/NDP. Log: `research/results/watch-large-linux.log`. Benchmark logs and `watch-render-summary.json` are under `research/results`; all are ignored development evidence.

## Fing static evidence and Shelly catalog — 2026-09-08

- Rechecked native Fing `liboverlook.dylib` and `fingagent.bin` hashes against the original inventory and inspected selected ARM64 routines without running the application. The OUI initializer loads a configurable properties file; the inspected catalog lookup calls through a remote client using user-profile context. No complete recognition database was recovered and no lookup service was called. Bounded findings and addresses are recorded in `research/fing-static-analysis.json`; raw disassembly remains ignored.
- Imported 155 exact Shelly model-name/generation records from aioshelly revision `8f1b2f9bf2fc154faf6c3d73213d59620a6e301c` under Apache-2.0. The importer validates pinned source/license hashes and extracts AST literals without executing upstream code. Rebuilding with the same retrieval date reproduced both index and metadata byte-for-byte. The upstream license and adaptation notice are included; the pinned repository has no separate top-level NOTICE file.
- Automatic Shelly catalog matches require both reported model and generation, keep the original model code, and attach product-name/brand claims only to the selected model source. Tests cover all 155 records, unknown/malformed codes, generation mismatch, returned-data ownership, conflicting selected identities, and isolation from Apple protocol fields. Generic offline lookup now covers 761 identifiers; `models sources` returns two provenance records while the original core `Provenance()` API retains AppleDB behavior.
- macOS ARM64 vet/race and IPv4/IPv6 socket integration passed, including actual split DNS-SD replies followed by a JSON identity read and catalog resolution. The full Linux ARM64 suite passed, including protocol, UI, installation, cancellation, snapshots, Samba, scoped neighbors, diagnostics, and ARP/NDP. Logs: `research/results/shelly-catalog-macos-network.log` and `shelly-catalog-linux.log`.
- All four local `catalog-check` archives passed checksum/architecture/license/provenance/offline-link verification and actual model/source lookups. Native macOS ARM64, Rosetta macOS x86-64, Linux ARM64 containers, and translated Linux x86-64 containers also passed their packaged JSONL/terminal/snapshot fixtures. These are local artifacts, not a public release or native Intel/AMD hardware verification. Runtime log: `research/results/shelly-catalog-packaged-runtime.log`; tested executable hashes: `shelly-catalog-binary-hashes.json`. Logs and archives remain ignored development evidence.

## Remote integration and authenticated installation — 2026-09-08

- Refreshed `origin/main` at `dd848a6`, confirmed it is an ancestor of the completed CLI work, and pushed the 19-commit integration at `43cab67` to `integration/cli-release-candidate`. Draft [PR #1](https://github.com/coupez/lantern/pull/1) presents the complete scope and validation limits. The repository remains private and no release was published. Tracked paths exclude downloaded/extracted applications, local scan reports, built artifacts, and credential-file extensions.
- An actual versioned `go install` fetched revision `43cab67f3491ab74b07301bafc208d0af2b0c92a` from private GitHub using existing authentication, outside the checkout with temporary GOPATH/GOBIN/module-cache directories. Go automatically downloaded toolchain 1.26.8 and dependencies. The installed binary reported `v0.0.0-20260908202020-43cab67f3491`, resolved Shelly Plus 1, returned both catalog provenance records, and ran the four-device demo. Temporary installation files were removed. Logs/results: `research/results/remote-versioned-install.log` and `remote-versioned-install.json` (ignored).
- The initial install attempt explicitly disabled toolchain switching and correctly refused to build with bootstrap Go 1.26.3. The passing run used automatic selection, matching the documented installation route. This verifies authenticated remote retrieval; it does not establish public availability or change global Go/Git configuration.

## Release candidate and hosted CI (2026-09-08)

- Draft [PR #1](https://github.com/coupez/lantern/pull/1) and the unpublished `v0.1.0-rc.1` release were staged at `3e7224439a7a77ee991dccd8d883ffdef52b1211`. GitHub-hosted macOS ARM64 and Linux AMD64 [CI passed](https://github.com/coupez/lantern/actions/runs/34275095578), including Linux protocol, raw ARP/NDP, denied-capability, terminal, installation, and archive checks. These are runner architecture observations, not physical LAN/device coverage.
- All four exact local RC packaged binaries passed their runtime fixtures (native macOS ARM64, Rosetta macOS x86-64, Linux ARM64 containers, translated Linux x86-64 containers). All four uploaded archives and the checksum file matched local SHA-256 hashes; authenticated draft downloads matched them again. Local evidence: `research/results/rc-packaged-runtime.log`, `rc-draft-release.json`, and `rc-remote-ci.json`/`.log` (ignored). The repository remains private, the PR is unmerged, and the release is unpublished. Later follow-up commits are not in these RC archives.

## Unambiguous UPnP identity fields (2026-09-08)

- Regression tests reproduced concatenation of repeated `friendlyName`, `manufacturer`, `modelName`, `modelNumber`, `deviceType`, and `UDN` fields. The parser now rejects repeated recognized fields, even when the first element is empty or the second uses an equivalent namespace prefix. Nested markup inside a recognized field also rejects the document instead of silently truncating its text. No partially parsed device identities escape a rejected document.
- Text split across CDATA/comments remains supported and obeys the accumulated field-size limit. Extension namespaces, nested extension fields, and separate embedded-device values remain isolated. Real IPv4/IPv6 loopback HTTP tests verify that ambiguous descriptions add no identity and retain the original SSDP advertisement.
- macOS ARM64 vet and the full race suite passed. Targeted description/Shelly transport race tests passed on macOS and Linux ARM64; Linux ran with `--network none --cap-drop ALL`. Logs: `research/results/upnp-fields-macos-network.log` and `upnp-fields-linux.log` (ignored). This validates controlled protocol handling, not physical UPnP device compatibility.

## WS-Discovery and ONVIF scope evidence (2026-09-08)

- Added parallel IPv4/IPv6 WS-Discovery 2005/04 and 2009/01 Probe passes to enumerated and sparse discovery. Both versions use distinct random request UUIDs, bounded retransmissions, a one-hop multicast destination, per-write deadlines, and the existing receive/cancellation window. Replies require matching discovery/addressing namespaces, action, and request correlation; duplicate responses are suppressed and advertised URLs never become targets, HTTP requests, or verified ports.
- Bounded XML parsing retains expanded QName types, endpoint references, scope/XAddr metadata, and metadata versions. Tests cover malformed/uncorrelated responses, duplicates, nested values, namespace aliases/rebinding, URI/name validation, list/match limits including discovery proxies, raw packet exhaustion, partial receive failures, short writes, and pre-cancellation. ONVIF name/hardware scopes retain separate advertised claims; hardware text does not create a model/manufacturer/catalog match.
- Actual IPv4/IPv6 UDP fixtures drop the initial transmission and first retry for both protocol versions. The final retransmission discovers the responder within the one-second window; wrong-correlation and duplicate packets are ignored. Cancellation closes the receiver and joins the retry worker.
- An isolated Linux IPv4 multicast responder joins UDP 3702 and answers the actual engine's Probes. Its identity appears in both `device_update` and the final report without creating an open port; all multicast passes together finish in approximately 1.01 seconds. This is a controlled local responder fixture, not a LAN discovery benchmark or physical endpoint verification. It runs automatically in the Linux container suite.
- macOS ARM64 vet/full race and all enabled network integration tests passed. The full Linux ARM64 race/protocol/CLI/ARP/NDP/Samba/terminal suite passed; final targeted Linux checks also cover the tightened proxy-inclusive match limit and multicast engine fixture. A ten-second parser fuzz run completed 463,408 executions without a failure. Local logs: `research/results/wsd-macos-all-network.log`, `wsd-macos-network.log`, `wsd-linux-runtime.log`, `wsd-linux-final.log`, and `wsd-fuzz.log` (ignored).
- References and supported limits are recorded in [discovery](discovery.md) and [recognition](recognition.md). No upstream implementation code or vendor-specific hardware catalog was imported. Physical Windows/printer/camera interoperability, managed discovery, SOAP 1.1, and HTTP metadata retrieval remain unverified or unsupported as documented. The staged `v0.1.0-rc.1` release predates this work.

## Independent wsdd interoperability (2026-09-08)

- Ran Debian Bookworm `wsdd` package `2:0.7.0-2.1` inside the disposable Linux ARM64 test container. Its actual Probe handler ignored Lantern's original untyped Probes because it requires `wsdp:Device`. The first three CLI scans received no WS-Discovery endpoint despite the independent daemon logging the incoming requests. Inspecting the installed package identified this requirement; no wsdd implementation code was copied into Lantern.
- Added typed DPWS Device Probes alongside the general queries, for both supported discovery versions. All four queries have independent random IDs and retain those IDs through two bounded retransmissions. The plan now sends at most twelve datagrams within the existing response window. Updated unit and IPv4/IPv6 loss/cancellation fixtures cover the expanded plan and its namespace/type declarations.
- The real CLI then discovered wsdd's stable endpoint over IPv4 and scoped IPv6, preserving its two expanded type names and fresh response IDs. The tested daemon omits XAddrs in ProbeMatches; Lantern still discovers it without Resolve or HTTP metadata exchange. Repeated scans return an empty snapshot diff, and disabling multicast removes WS-Discovery evidence. No hostname/model/manufacturer or open TCP port is invented from those types. Observed IPv4 scan duration was 1,005 ms.
- IPv4 wsdd tests run with all container capabilities removed. IPv6 creates and deletes an isolated veth pair using only `NET_ADMIN`; raw sockets are unnecessary. The fixture disables automatic link-local address generation and adds non-tentative test addresses. An initial attempt exposed a separate existing limitation: multicast source selection can choose a still-tentative automatically generated IPv6 address and report bind failures. That general source-selection issue remains recorded in goal status; deterministic fixture setup does not establish that it is fixed.
- macOS ARM64 vet/full race and the IPv4/IPv6 WS-Discovery socket tests passed. The complete Linux suite now includes both independent wsdd cases and passed with the four-query plan. Logs: `research/results/wsdd-ipv4.log`, `wsdd-ipv6.log`, `wsdd-macos-network.log`, and `wsdd-linux-full.log` (ignored). The fixture owns and terminates the daemon and removes temporary links/files; it runs no HTTP metadata service and exposes no host mount or published port. This verifies interoperability with wsdd 0.7.0, not physical Windows/printer/camera hardware.

## Multicast source-address recovery (2026-09-08)

- mDNS, SSDP, WS-Discovery, and `doctor` now share multicast source selection that tries matching addresses on the originally selected interface. Only a listen/bind failure carrying `EADDRNOTAVAIL` permits another candidate; permission/configuration failures stop immediately. All attempts consume one deadline, and cancellation prevents further attempts. Success retains its normal owned socket cleanup; if all sources fail, the error remains visible. No fallback crosses to another interface.
- Unit tests cover second-source success, deadline consumption, cleanup ownership, exhausted candidates, permission/non-bind failures, cancellation between attempts, and an already-expired deadline. macOS ARM64 vet/full race and all enabled socket integration tests passed.
- The independent Linux IPv6 wsdd fixture creates a real failed-DAD address, verifies it is first in the kernel address inventory, and confirms an actual UDP bind fails with `EADDRNOTAVAIL`. The usable second address then supports successful CLI discovery and an available `doctor` multicast check. The fixture runs using only `NET_ADMIN`, with no raw socket capability, and is included in the regular Linux suite.
- A binary built from pre-fix commit `3c4051a` failed the same corrected fixture: all three multicast passes reported bind failures on `fe80::bad%wsd0`, and the report lacked WS-Discovery evidence. The fixed binary passed. The first fixture iteration did not force the rejected address first and passed both versions; it was corrected before this comparison. Logs: `research/results/multicast-source-baseline.log`, `multicast-source-linux.log`, `multicast-source-macos-network.log`, and `multicast-source-linux-full.log` (ignored).
- The full Linux ARM64 protocol/CLI/ARP/NDP/Samba/wsdd/terminal suite passed. This resolves the previously recorded multicast bind fallback issue; live macOS duplicate-address detection and broader physical-network transition coverage remain unverified. The staged release candidate predates this fix.

## Watch search across identity evidence (2026-09-08)

- Watch search now includes identity claim fields and values, including ONVIF hardware descriptions, unselected model claims, and firmware-build strings. Selected display identities remain unchanged. Regression tests reproduced missing matches before the change and verify case-insensitive matching, report replacement, stale-claim removal, and independent input ownership afterward.
- Search text is still built only for an active query and reused until the device/report changes. A string builder prevents repeated copying per claim. The 1,024-device/64-claim benchmark and its measured first-index/query/redraw costs are documented in [performance](performance.md).
- macOS ARM64 UI vet/race and actual watch pseudo-terminal checks passed. Linux ARM64 UI race and the same terminal checks passed in a local container with `--network none --cap-drop ALL`. Logs: `research/results/watch-claims-macos-pty.log` and `watch-claims-linux.log` (ignored). Local `v0.1.0-rc.2` archives were prepared before this UI change; no updated artifact or source upload occurred.

## Snapshot device-key validation (2026-09-08)

- Baseline regressions showed that snapshots with missing or repeated device addresses loaded successfully; repeated addresses silently replaced earlier records in the comparator's IP map. Saving an unsupported schema could also replace an existing file. Save and Load now share schema/device-key validation, with zero reports on load failure and validation before save-file creation. Legacy optional metadata, extension fields, duplicate port observations, and distinct IPv6 zones remain supported.
- Unit cases cover missing/null, unspecified (including scoped unspecified IPv6), multicast, mapped IPv6, duplicate IPv4, equivalent IPv6 spellings, and repeated same-zone addresses. Rejected saves preserve destination bytes and leave no temporary files. The actual CLI rejects bad inputs on either side of a diff with status 1, a file-specific error, no stdout, and unchanged inputs.
- Enumeration excludes unspecified addresses so `0.0.0.0/31` and `::/127` each fit a one-host limit. Injected engine probes verify only the eligible address is contacted and the resulting report survives Save/Load without comparison changes.
- macOS ARM64 vet, all race tests, and the rebuilt CLI snapshot fixture passed. The full Linux ARM64 suite passed, including race/vet, protocol sockets, installation/core imports, full-port scanning, JSONL cleanup, terminal/snapshot checks, Samba/wsdd, interface isolation, and ARP/NDP. Log: `research/results/snapshot-validation-linux.log` (ignored). Prepared rc.2 archives predate this change; no remote upload occurred. Direct callers of `Diff` remain responsible for valid, unique device keys; this is not full report-field validation.

## Matter DNS-SD recognition (2026-09-08)

- Direct queries now cover commissionable, commissioner, and operational services under the existing mDNS deadline and collection caps. Commissioning name/vendor-product/type fields become source-linked claims; operational advertisements do not interpret those fields. No pairing, control, extra HTTP requests, retail-model/manufacturer guesses, or MAC/open-port inference is added.
- The embedded category table contains 65 standard device-type labels from Apache-2.0 connectedhomeip v1.4.0.0, revision `43aa98c2d30ee547c6b587b9de7bbb794f175ece`. Utility labels remain claims; application types can supply a display hint. The importer reproduces identical bytes and rejects a modified XML input without replacing the table. Source/license/notice hashes and selection rules are recorded in the generated JSON; upstream copyright, notice, and license accompany releases.
- Tests cover numeric widths and syntax, unknown IDs, vendor-only IDs, name byte/UTF-8 limits, exact service/protocol matching, operational-field isolation, duplicate/order-independent claims, conflicting categories, and utility types. macOS ARM64 vet/all race tests passed. Controlled split-reply IPv4/IPv6 sockets discover all three services even without service enumeration. Log: `research/results/matter-macos-network.log` (ignored).
- The actual macOS CLI also passed against three independently generated native Bonjour proxy services restricted to loopback. It retained name/type/vendor/product claims and matching saved JSON without inventing a model, manufacturer, MAC, or open port; disabling multicast removed the synthetic observations. Fixture setup confirms the registered address records before scanning and includes the publisher in the target to satisfy sender filtering. Log: `research/results/matter-bonjour.log`. The script is included in macOS CI configuration; no updated remote CI run has occurred.
- The full Linux ARM64 suite passed, including protocol sockets, race/vet, installation/core imports, JSONL cleanup, full-port scanning, terminal/snapshot fixtures, Samba/wsdd, scoped interfaces, and ARP/NDP. All four local `matter-check` archives passed checksums, executable architecture, bundled provenance/licenses/offline links, and actual binary CLI fixtures on native macOS ARM64, Rosetta macOS x86-64, Linux ARM64 containers, and translated Linux x86-64 containers. Logs: `research/results/matter-linux.log` and `matter-packaged-runtime.log` (ignored).
- These are controlled DNS-SD interoperability checks, not physical Matter-device, Thread forwarding, sleepy-node, or certification evidence. Product/vendor-name catalogs remain absent. Prepared rc.2 archives predate this feature; nothing was uploaded.
